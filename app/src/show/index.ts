export enum ERoute {
  login = "/login",
  home = "/home",
  menu = "/menu",
  reward = "/reward",
  register = "/register",  
  registerMovie = "/registermovie",  
  chat = "/chat",
}

const route = ERoute;
export default {
  route,
};
